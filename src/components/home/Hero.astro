---
const heroImg = "/img/hero-robot.jpg"; // reemplaza con tu imagen estilo mockup
---

<section class="hero">
  <div class="hero__bg" style={`background-image:url(${heroImg})`}>
    <div class="container hero__inner">
      <div class="hero__text" data-reveal>
        <h1>El puente entre<br/>inteligencia artificial,<br/>robótica y procesos<br/>automatizados.</h1>
        <div class="hero__chips">
          <span class="chip">Respuesta rápida & SLA</span>
          <span class="chip">Enfoque absoluto en seguridad</span>
          <span class="chip">Acompañamiento postventa</span>
        </div>
      </div>
    </div>
  </div>

  <style>
    .hero__bg{
      min-height: clamp(420px, 56vh, 620px);
      background-size: cover;
      background-position: center;
      position: relative;
      border-bottom: 1px solid rgba(0,0,0,.08);
    }
    .hero__bg::before{
      content:"";
      position:absolute; inset:0;
      background: linear-gradient(90deg, rgba(0,0,0,.62) 0%, rgba(0,0,0,.18) 55%, rgba(0,0,0,.06) 100%);
    }
    .hero__inner{
      position:relative;
      min-height: inherit;
      display:flex;
      align-items:center;
      padding: 34px 0;
    }
    .hero__text{ max-width: 560px; }
    h1{
      margin:0 0 16px 0;
      color:#fff;
      font-size: clamp(2.0rem, 3.3vw, 3.0rem);
      line-height:1.05;
      letter-spacing:.2px;
      font-weight:800;
    }
    .hero__chips{ display:flex; flex-wrap:wrap; gap:10px; }
    @media (max-width: 820px){
      .hero__bg::before{
        background: linear-gradient(180deg, rgba(0,0,0,.62) 0%, rgba(0,0,0,.20) 70%, rgba(0,0,0,.05) 100%);
      }
    }
  </style>
</section>
